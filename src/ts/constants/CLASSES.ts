/** Added to `<html>` for the lifetime of a coordinator-driven morph. */
export const CLASS_MORPHING = 'arts-cs-morphing'

/** Present while the native CSS scroll tier owns the scalar. */
export const CLASS_SCROLL_NATIVE = 'arts-cs-scroll-native'

/** Flips the native tier's keyframes when the scrub runs toward Default. */
export const CLASS_SCROLL_REVERSE = 'arts-cs-scroll-reverse'
