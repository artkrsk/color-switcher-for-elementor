/** Element setting keys — mirrored from PHP Managers\Elements (parity-pinned). */
export const SETTING_ENABLED = 'arts_cs_enabled'
export const SETTING_VIEWPORT = 'arts_cs_viewport'

/** Both states carry a value: an empty one would inherit across breakpoints. */
export const ENABLED_SWITCH = 'switch'

/** Document setting key — mirrored from PHP Managers\Documents. */
export const SETTING_PAGE_THEME = 'arts_cs_page_theme'
