/** Cookie holding the visitor's preference. Read by the head script too. */
export const COOKIE_PREFERENCE = 'arts_cs_pref'

/** A year, in seconds — long enough that a choice survives, short enough to lapse. */
export const COOKIE_MAX_AGE = 31536000

export const PREFERENCE_SYSTEM = 'system'

/** The query every resolution of `system` goes through. */
export const QUERY_DARK = '(prefers-color-scheme: dark)'
